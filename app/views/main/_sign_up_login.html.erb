<div class="mt-5 sm:mx-auto sm:w-full sm:max-w-sm flex flex-row justify-around">
    <%= button_to("Register", new_user_registration_path, method: :get, :class => "bg-secondary hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" )%>
    <%= button_to("Login", new_user_session_path, method: :get, :class => "bg-secondary-50 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded")%> 
</div>

