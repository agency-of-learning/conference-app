<div class="w-full">
  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Speakers</h1>

    <% if current_user&.admin? %> <%# temporary fix %>
      <%= link_to 'New speaker', new_speaker_path, class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium" %>
    <% end %>
  </div>

  <div id="speakers" class="min-w-full">
    <%= render @speakers %>
  </div>
</div>
