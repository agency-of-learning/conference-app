<div data-controller="tabs" data-tabs-active-tab="-mb-px border-l border-t border-r rounded-t">
  <ul class="list-reset flex border-b">
    <li class="-mb-px mr-1" data-tabs-target="tab" data-action="click->tabs#change">
      <a class="bg-white inline-block py-2 px-4 text-blue-500 hover:text-blue-700 font-semibold no-underline" href="#">Wed, Oct 4</a>
    </li>
    <li class="mr-1" data-tabs-target="tab" data-action="click->tabs#change">
      <a class="bg-white inline-block py-2 px-4 text-blue-500 hover:text-blue-700 font-semibold no-underline" href="#">Thurs, Oct 5</a>
    </li>
    <li class="mr-1" data-tabs-target="tab" data-action="click->tabs#change">
      <a class="bg-white inline-block py-2 px-4 text-blue-500 hover:text-blue-700 font-semibold no-underline" href="#">Fri, Oct 6</a>
    </li>
  </ul>

  <div class="hidden py-4 px-4 border-l border-b border-r" data-tabs-target="panel">
    <div id="talks" class="min-w-full">
      <% @day_one_talks.each do |talk| %>
        <%= render "talks/talk", talk: talk %>
      <% end %>
    </div>
  </div>

  <div class="hidden py-4 px-4 border-l border-b border-r" data-tabs-target="panel">
    <div id="talks" class="min-w-full">
      <% @day_two_talks.each do |talk| %>
        <%= render "talks/talk", talk: talk %>
      <% end %>
    </div>
  </div>

  <div class="hidden py-4 px-4 border-l border-b border-r" data-tabs-target="panel">
    <div id="talks" class="min-w-full">
      <% @day_three_talks.each do |talk| %>
        <%= render "talks/talk", talk: talk %>
      <% end %>
    </div>
  </div>
</div>