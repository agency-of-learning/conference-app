<div data-controller="tabs" data-tabs-active-tab="bg-secondary focus:bg-secondary text-white text-l" data-tabs-inactive-tab="bg-secondary-50 text-secondary">
  <ul class="list-reset flex justify-center mb-5">
    <li class="w-full text-center rounded" data-tabs-target="tab" data-action="click->tabs#change" data-tabs-index-value="1">
      <a class="inline-block py-4 px-1 text-base font-black italic no-underline" href="#">Wed, Oct 4</a>
    </li>
    <li class="w-full text-center rounded" data-tabs-target="tab" data-action="click->tabs#change">
      <a class="inline-block py-4 px-1 text-base font-black italic no-underline" href="#">Thurs, Oct 5</a>
    </li>
    <li class="w-full text-center rounded mr-1" data-tabs-target="tab" data-action="click->tabs#change">
      <a class="inline-block py-4 px-1 text-base font-black italic no-underline" href="#">Fri, Oct 6</a>
    </li>
  </ul>

  <div class="hidden py-4" data-tabs-target="panel">
    <div id="talks" class="min-w-full">
      <% @day_one_talks.each do |talk| %>
        <%= render "shared/my_schedule_talk", talk: talk %>
      <% end %>
    </div>
  </div>

  <div class="hidden py-4" data-tabs-target="panel">
    <div id="talks" class="min-w-full">
      <% @day_two_talks.each do |talk| %>
        <%= render "shared/my_schedule_talk", talk: talk %>
      <% end %>
    </div>
  </div>

  <div class="hidden py-4" data-tabs-target="panel">
    <div id="talks" class="min-w-full">
      <% @day_three_talks.each do |talk| %>
        <%= render "shared/my_schedule_talk", talk: talk %>
      <% end %>
    </div>
  </div>
</div>