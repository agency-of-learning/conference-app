<div data-controller="tabs" data-tabs-active-tab="bg-secondary focus:bg-secondary text-white text-xl" data-tabs-inactive-tab="bg-secondary-50">
  <ul class="list-reset flex justify-center mb-5">
    <li class="w-full text-center" data-tabs-target="tab" data-action="click->tabs#change" data-tabs-index-value="1">
      <a class="inline-block py-4 px-10 text-white text-l font-bold no-underline" href="#">Upcoming</a>
    </li>
    <li class="w-full text-center" data-tabs-target="tab" data-action="click->tabs#change">
      <a class="inline-block py-4 px-10 text-white text-l font-bold no-underline" href="#">Past</a>
    </li>
  </ul>

  <div class="hidden py-4 px-4 border-l border-b border-r" data-tabs-target="panel">

    <div>
      <% if @day_one_talks.any? %>
        <h2 class="font-bold text-xl">Wed, Oct 4</h2>
      <% end %>
    </div>

    <div id="talks" class="min-w-full">
      <% @day_one_talks.each do |talk| %>
        <%= render "talks/talk", talk: talk %>
      <% end %>
    </div>

    <div>
      <% if @day_two_talks.any? %>
        <h2 class="font-bold text-xl">Thurs, Oct 5</h2>
      <% end %>
    </div>

    <div id="talks" class="min-w-full">
      <% @day_two_talks.each do |talk| %>
        <%= render "talks/talk", talk: talk %>
      <% end %>
    </div>

    <div>
      <% if @day_three_talks.any? %>
        <h2 class="font-bold text-xl">Fri, Oct 6</h2>
      <% end %>
    </div>
    <div>
      <% @day_three_talks.each do |talk| %>
        <%= render "talks/talk", talk: talk %>
      <% end %>
    </div>

  </div>
  
  <div class="hidden py-4 px-4 border-l border-b border-r" data-tabs-target="panel">

  </div>

</div>