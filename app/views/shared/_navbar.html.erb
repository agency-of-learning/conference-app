<nav class="font-sans flex flex-col text-center sm:flex-row sm:text-left sm:justify-between py-4 px-6 bg-white shadow sm:items-baseline w-full">
  <div class="mb-2 sm:mb-0">
    <a href="/" class="text-2xl no-underline text-grey-darkest hover:text-blue-dark">
    <% if current_user  %>
        Home
    <% else %>
        Please Login
    <% end %>
    </a>
  </div>
  <div class="flex flex-row">
    <% if current_user %>
        <span class="text-lg no-underline text-grey-darkest hover:text-blue-dark mx-2 py-2 px-4">Welcome <%= "#{current_user.role}" %>!</span>
        <%= button_to("Logout", destroy_user_session_path, method: :delete, class: "bg-pink-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded")%>
    <% end %>
  </div>
</nav>