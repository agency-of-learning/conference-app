<nav class="navbar bg-base-100 p-4 bg-red-600 flex justify-between">
  <div class="mb-2 sm:mb-0  items-center">
    <a href="/" class="text-2xl no-underline text-grey-darkest hover:text-blue-dark">
    <img class="h-4 w-full grow "src="https://rubyonrails.org/assets/world/rails-world-logo.png">
    </a>
  </div>

  <div>
    <ul class="menu menu-horizontal px-1 hidden lg:flex space-x-2 items-center">
        <% if current_user %>
            <li><span class="text-lg no-underline text-white font-extrabold italic mx-2 py-2 px-4">Welcome <%= "#{current_user.role}" %>!</span>
            </li>
            <li>
            <%= button_to("Logout", destroy_user_session_path, method: :delete, class: "bg-pink-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded")%>
            </li>
        <% end %>
    </ul>

    <div class="dropdown dropdown-end flex lg:hidden">
     <% if current_user %>
      <label tabindex="0" class="btn btn-ghost">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h8m-8 6h16" /></svg>
      </label>
      <ul tabindex="0" class="menu menu-compact dropdown-content mt-3 p-2 shadow bg-base-100 rounded-box w-52">
            <li>
            <%= button_to("Logout", destroy_user_session_path, method: :delete, class: "bg-pink-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded")%>
            </li>
       
      </ul>
       <% end %>
    </div>
  </div>
  
</nav>